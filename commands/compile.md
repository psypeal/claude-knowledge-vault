---
description: Compile pending sources into wiki articles
argument-hint: "[source-slug]"
---

Follow the **vault-operations** skill's compile procedure.

If `$ARGUMENTS` names a specific source slug, compile only that source. Otherwise compile all pending sources into wiki articles.
