---
description: Add a raw source to the vault
argument-hint: <url|text|filepath>
---

Follow the **vault-operations** skill's ingest procedure.

Accept one of: URL, file path, pasted text, or MCP tool output. The source is provided in `$ARGUMENTS`.

Create the appropriate raw source entry and metadata in `.vault/raw/`.
