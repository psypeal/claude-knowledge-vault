---
description: Find full text for reference-only vault items
argument-hint: "[slug or --all]"
disable-model-invocation: true
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/knowledge-vault/SKILL.md`, then follow its `enrich-references` workflow with `$ARGUMENTS`.
