---
description: Initialize a knowledge vault in the current project
disable-model-invocation: true
---

Read `${CLAUDE_PLUGIN_ROOT}/skills/knowledge-vault/SKILL.md`, then follow its `init` workflow with `$ARGUMENTS`.
